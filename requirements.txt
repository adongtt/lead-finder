requests>=2.31.0
pyyaml>=6.0.1

# Search engines (pick one or both):
ddgs>=8.0.0                    # Fast, free, no browser needed (new package name)
playwright>=1.40.0             # Real browser automation (fallback)

# Web API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
bcrypt>=4.0.0
itsdangerous>=2.0.0
psycopg2-binary>=2.9.0
openpyxl>=3.1.0

